export declare class Configuration {
    bootstrap_url: string;
    client_id: string;
    account: string;
    force_local: boolean;
    constructor(bootstrap_url: string, client_id: string, account: string, force_local?: boolean);
}
export declare class Urls {
    canonical: string;
    websocket: string;
    proxy: string;
    constructor(canonical?: string, websocket?: string, proxy?: string);
}
export declare class Authentication {
    token: string;
    jwt: string;
    oauth: string;
    username: string;
    constructor(token?: string, jwt?: string, oauth?: string, username?: string);
}
