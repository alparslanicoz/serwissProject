"use strict";
var Configuration = (function () {
    function Configuration(bootstrap_url, client_id, account, force_local) {
        this.bootstrap_url = bootstrap_url;
        this.client_id = client_id;
        this.account = account;
        this.force_local = force_local;
    }
    return Configuration;
}());
exports.Configuration = Configuration;
var Urls = (function () {
    function Urls(canonical, websocket, proxy) {
        this.canonical = canonical;
        this.websocket = websocket;
        this.proxy = proxy;
    }
    return Urls;
}());
exports.Urls = Urls;
var Authentication = (function () {
    function Authentication(token, jwt, oauth, username) {
        this.token = token;
        this.jwt = jwt;
        this.oauth = oauth;
        this.username = username;
    }
    return Authentication;
}());
exports.Authentication = Authentication;
//# sourceMappingURL=configuration.js.map