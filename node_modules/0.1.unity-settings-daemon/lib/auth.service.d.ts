import { ConfigurationService } from './config.service';
import { Authentication } from './configuration';
import { Http } from '@angular/http';
import { EventEmitter } from '@angular/core';
import { JwtHelper } from 'angular2-jwt';
export declare class AuthService {
    http: Http;
    config: ConfigurationService;
    timerRefreshToken: any;
    auth: Authentication;
    jwtHelper: JwtHelper;
    loggedin: EventEmitter<{}>;
    constructor(http: Http, config: ConfigurationService);
    save_auth(): void;
    login_oauth(user: any, password: any): void;
    call_real_oauth(response: any, user: any, password: any): void;
    saveUserToken(res: any, refresh: any): void;
    refreshToken(endpoint: any): void;
}
